<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <title>Stats — <%= link.code %></title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100 min-h-screen">

  <div class="max-w-2xl mx-auto py-12">

    <div class="bg-white shadow-lg p-8 rounded-xl">
      <h1 class="text-3xl font-semibold text-indigo-600 mb-5">
        Stats for <%= link.code %>
      </h1>

      <div class="space-y-3 text-lg">
        <p>
          <span class="font-bold">Short URL:</span>
          <a href="<%= baseUrl %>/<%= link.code %>"
            class="text-indigo-600 underline" target="_blank">
            <%= baseUrl %>/<%= link.code %>
          </a>
        </p>

        <p>
          <span class="font-bold">Target URL:</span>
          <a href="<%= link.targetUrl %>"
            class="text-gray-700 underline" target="_blank">
            <%= link.targetUrl %>
          </a>
        </p>

        <p>
          <span class="font-bold">Total Clicks:</span>
          <%= link.totalClicks %>
        </p>

        <p>
          <span class="font-bold">Last Clicked:</span>
          <%= link.lastClicked ? link.lastClicked : "Never" %>
        </p>

        <p>
          <span class="font-bold">Created At:</span>
          <%= link.createdAt.toISOString() %>
        </p>
      </div>

      <a href="/"
        class="block mt-6 bg-indigo-600 text-white text-center p-3 rounded-lg hover:bg-indigo-700 transition">
        Back to Dashboard
      </a>
    </div>

  </div>
</body>

</html>
